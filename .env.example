# LLM provider (Phase 0 does not call the LLM)
LLM_MODEL=deepseek-chat
LLM_BASE_URL=https://api.deepseek.com
LLM_API_KEY=

# Paths (relative to project root)
DATA_DIR=data
DB_PATH=data/industrial.db
TRACES_DIR=traces

# Agent defaults
DEFAULT_WORK_ORDER_LOOKUP_DAYS=30
